{% extends 'base.html.twig' %}

{% block title %}Apteczka{% endblock %}

{% block body %}
    <h1>Apteczka</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ apteczka.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ apteczka.nazwa }}</td>
            </tr>
        </tbody>
    </table>

    <table class="table">
    <tr>
    <th>
    Nazwa
    </th>
    <th>
    Data waznosci
    </th>
    <th>
    Ilosc
    </th>
    </tr>
    {% for lek in apteczka.medykamenty %}

    
    <tr>
    <td>
{{lek.slowniklekow.nazwahadlowa}}
    </td>
    <td>
{{lek.datawaznosci|date}}
    </td>
    <td>
{{lek.ilosc}}
    </td>
    <td>
    <form method="post" action="{{ path('medykament_utylizuj', {'id': lek.id}) }}" onsubmit="return confirm('Zutylizować?');">
   <input type="text" name="liczba" placeholder="liczba" />
    <button class="btn">Utylizuj</button>
</form>

<form method="post" action="{{ path('medykament_wydaj', {'id': lek.id}) }}" onsubmit="return confirm('Wydać?');">
    <input type="text" name="liczba" placeholder="liczba" />
    <button class="btn">Wydaj</button>
</form>

    </td>
    </tr>
{% endfor %}
    </table>
    <a href="{{ path('apteczka_index') }}">back to list</a>

    <a href="{{ path('apteczka_edit', {'id': apteczka.id}) }}">edit</a>

    {{ include('apteczka/_delete_form.html.twig') }}
{% endblock %}
